{
  "name": "free-geez-fonts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/download.js && node --check api/download-stats.js && node --check assets/js/app.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.5.0"
  }
}
